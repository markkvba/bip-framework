package gov.va.ocp.framework.test.service;

//@RunWith(MockitoJUnitRunner.class)
public class BearerTokenServiceTest {

	private BearerTokenService subject;
	
	/*
	
	@Mock
	RESTUtil restUtil;
	
	@Test
	public void testreadFile_Success() {
		MockitoAnnotations.initMocks(this);
		System.setProperty("test.env", "qa");
		//RESTUtil restUtil = Mockito.mock(RESTUtil.class);
		when(restUtil.postResponse(Mockito.anyString())).thenReturn("Some Bearer Token");
		String token = BearerTokenService.getInstance().getBearerToken();
		System.out.println("-------------"+token);
		
	}
*/
}
